.venv/
__pycache__/
*.py[cod]
.DS_Store
.vscode/
.pytest_cache/
.ruff_cache/

# Secrets
.env
.env.local
backend/.env
deploy/.env

# Backend
backend/.venv/
backend/staticfiles/
backend/media/
# Workbooks and text feeds the exporter writes. Deliberately outside media/ - see
# EXPORT_DIR in backend/config/settings/base.py. The bare pattern matches any depth, so it
# also covers the orphaned root var/ left behind by the pre-Django pipeline.
var/
backend/.coverage
htmlcov/

# Frontend
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.env*.local

graphify-out/
