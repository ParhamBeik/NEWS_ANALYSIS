# Django 5.2 is the LTS line, security-supported to April 2028. 5.1 reached end of life in
# December 2025 and stopped receiving security patches, which is what forced this move.
# Staying on LTS rather than tracking 6.x means one planned upgrade every two years instead
# of one every eight months, on a deployment with a single operator. django-celery-beat
# pins Django<6.1 anyway, so it is the package that decides when the 6.x jump can happen.
Django==5.2.17
djangorestframework==3.18.0
django-cors-headers==4.9.0
django-celery-beat==2.9.0
django-filter==26.1

psycopg[binary]==3.3.5
pgvector==0.5.0

celery[redis]==5.6.3
# NOT the latest (8.1.0). kombu, which celery vendors its transport through, declares
# `redis<6.5`, so anything above 6.4.0 is a resolver conflict rather than a choice.
redis==6.4.0

requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.3
PyYAML==6.0.3

pydantic==2.13.5
jdatetime==6.1.0
openpyxl==3.1.5
Pillow==12.3.0

gunicorn==26.2.0
whitenoise==6.12.0
